# Avionics Systems Emulator

This project simulates how avionics components (FMS, EFIS, GPS, Autopilot) communicate over a shared data bus, with a graphical interface built using PyQt5.

## Requirements

```bash
pip install PyQt5
